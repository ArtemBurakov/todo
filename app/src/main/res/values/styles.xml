<resources>
    <!-- Light application theme -->
    <style name="LightTheme" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="bottomNavigationColor">@color/bottomNavColor</item>
        <item name="contentTextColor">@color/contentTextColor</item>

        <item name="home_selector">@drawable/home_selector</item>
        <item name="dashboard_selector">@drawable/dashboard_selector</item>
        <item name="create_selector">@drawable/create_selector</item>
        <item name="notification_selector">@drawable/notification_selector</item>
        <item name="setting_selector">@drawable/setting_selector</item>

        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- Dark application theme -->
    <style name="DarkTheme" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">@color/contentBodyColorDark</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkMode</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="bottomNavigationColor">@color/bottomNavColorDark</item>
        <item name="contentTextColor">@color/contentTextColorDark</item>

        <item name="home_selector">@drawable/home_selector_white</item>
        <item name="dashboard_selector">@drawable/dashboard_selector_white</item>
        <item name="create_selector">@drawable/create_selector_white</item>
        <item name="notification_selector">@drawable/notification_selector_white</item>
        <item name="setting_selector">@drawable/setting_selector_white</item>

        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- Status bar -->
    <style name="NoStatusBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

</resources>